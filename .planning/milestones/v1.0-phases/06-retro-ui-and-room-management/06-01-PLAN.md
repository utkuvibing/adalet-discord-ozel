---
phase: 06-retro-ui-and-room-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/theme.ts
  - src/renderer.ts
  - src/index.css
  - src/renderer/components/JoinServer.tsx
  - src/renderer/components/Lobby.tsx
  - src/renderer/components/RoomList.tsx
  - src/renderer/components/ChatPanel.tsx
  - src/renderer/components/VoiceControls.tsx
  - src/renderer/components/RoomMembers.tsx
  - src/renderer/components/InvitePanel.tsx
  - src/renderer/components/VolumePopup.tsx
  - src/renderer/components/ConnectionToast.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Every screen in the app uses Inter font instead of Courier New / monospace"
    - "All border-radius values are 8px (main) or 6px (small elements) -- no 3px or 4px left"
    - "No component has fontFamily: 'monospace' or fontFamily: \"'Courier New'\" in its styles object"
    - "The color palette is consistent: #0d0d0d/#111111/#141414/#1a1a1a backgrounds, #7fff00 accent, #e0e0e0 text"
    - "The app looks like a polished dark Discord-like UI with neon green accent, not a retro terminal"
  artifacts:
    - path: "src/renderer/theme.ts"
      provides: "Shared theme constants for colors, font sizes, and border radius"
      exports: ["theme"]
    - path: "src/index.css"
      provides: "Global Inter font-family, updated input/button base styles with 8px border-radius"
      contains: "Inter"
    - path: "src/renderer.ts"
      provides: "Fontsource CSS imports for Inter weights 400, 500, 600, 700"
      contains: "@fontsource/inter"
  key_links:
    - from: "src/renderer.ts"
      to: "@fontsource/inter CSS"
      via: "import statements that Vite bundles into the renderer"
      pattern: "@fontsource/inter"
    - from: "src/renderer/theme.ts"
      to: "All 9 component style objects"
      via: "import { theme } in each component"
      pattern: "import.*theme"
    - from: "src/index.css"
      to: "body font-family"
      via: "CSS rule setting Inter as base font"
      pattern: "font-family.*Inter"
---

<objective>
Discord Dark Modern UI refresh: install Inter font via @fontsource, create shared theme constants, update global CSS and all 9 component style objects to use Inter font, 8px border-radius, and polished dark modern aesthetic.

Purpose: Delivers UI-01 -- the entire app uses a cohesive Discord Dark Modern visual style with neon green accent instead of the current monospace/terminal look.
Output: Theme constants file, fontsource imports, updated index.css, and all 9 components restyled with Inter font, 8px radius, and refined spacing.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-retro-ui-and-room-management/06-CONTEXT.md
@.planning/phases/06-retro-ui-and-room-management/06-RESEARCH.md

@src/renderer.ts
@src/index.css
@src/renderer/components/JoinServer.tsx
@src/renderer/components/Lobby.tsx
@src/renderer/components/RoomList.tsx
@src/renderer/components/ChatPanel.tsx
@src/renderer/components/VoiceControls.tsx
@src/renderer/components/RoomMembers.tsx
@src/renderer/components/InvitePanel.tsx
@src/renderer/components/VolumePopup.tsx
@src/renderer/components/ConnectionToast.tsx

<interfaces>
<!-- Locked decisions from CONTEXT.md that MUST be followed exactly. -->

Font: Inter (modern sans-serif) via @fontsource/inter npm package. NOT Google Fonts CDN.
Accent color: Keep #7fff00 neon green. No change.
CRT/scanline effects: NO.
Border radius: 8px main, 6px for small elements.
Styling pattern: Inline React.CSSProperties per component. Do NOT switch to CSS modules or styled-components.

Color Palette:
```
Background:   #0d0d0d (darkest), #111111 (sidebar), #141414 (cards), #1a1a1a (inputs)
Text:         #e0e0e0 (primary), #b0b0b0 (secondary), #888 (muted), #555 (disabled)
Accent:       #7fff00 (primary green), #99ff33 (hover green)
Error:        #ff4444
Warning:      #ff8800
Borders:      #2a2a2a (subtle), #3a3a3a (inputs)
```

Existing component style objects use `fontFamily: 'monospace'` or `fontFamily: "'Courier New', Courier, monospace"` -- ALL must be removed after body sets Inter globally.
Existing borderRadius values of '3px' and '4px' must ALL be updated to '8px' (or '6px' for small badge/tag elements).

Each component has a `const styles: Record<string, React.CSSProperties> = { ... }` block that must be updated in-place.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @fontsource/inter, create theme constants file, update renderer entry with font imports, and update global CSS</name>
  <files>src/renderer/theme.ts, src/renderer.ts, src/index.css</files>
  <action>
**Step 1: Install @fontsource/inter**

Run: `npm install @fontsource/inter`

This bundles Inter font files locally for Electron (no network dependency).

**Step 2: Create src/renderer/theme.ts**

Create a new file with shared theme constants. This does NOT violate the inline CSSProperties decision -- it just centralizes color/font/radius values so components reference `theme.colors.accent` instead of hard-coded `'#7fff00'`.

```typescript
export const theme = {
  colors: {
    bgDarkest: '#0d0d0d',
    bgSidebar: '#111111',
    bgCard: '#141414',
    bgInput: '#1a1a1a',
    textPrimary: '#e0e0e0',
    textSecondary: '#b0b0b0',
    textMuted: '#888',
    textDisabled: '#555',
    accent: '#7fff00',
    accentHover: '#99ff33',
    error: '#ff4444',
    warning: '#ff8800',
    borderSubtle: '#2a2a2a',
    borderInput: '#3a3a3a',
  },
  font: {
    family: "'Inter', sans-serif",
    sizeXs: '0.7rem',
    sizeSm: '0.8rem',
    sizeMd: '0.9rem',
    sizeLg: '1rem',
    sizeXl: '1.2rem',
    sizeTitle: '1.8rem',
  },
  radius: '8px',
  radiusSm: '6px',
} as const;
```

**Step 3: Update src/renderer.ts**

Add fontsource CSS imports at the top of the file, BEFORE the React import:

```typescript
import '@fontsource/inter/400.css';
import '@fontsource/inter/500.css';
import '@fontsource/inter/600.css';
import '@fontsource/inter/700.css';
```

Keep the existing imports and createRoot code unchanged.

**Step 4: Update src/index.css**

Replace the current body font-family from `'Courier New', Courier, monospace` to `'Inter', sans-serif`.

Update input base styles:
- Change `font-family` to `'Inter', sans-serif`
- Change `border-radius` from `4px` to `8px`

Update button base styles:
- Change `font-family` to `'Inter', sans-serif`

Keep everything else (scrollbar styling, focus states, keyframes) unchanged.

Full updated index.css:
```css
*, *::before, *::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  padding: 0;
  background-color: #0d0d0d;
  color: #e0e0e0;
  font-family: 'Inter', sans-serif;
  overflow: hidden;
}

::-webkit-scrollbar {
  width: 6px;
}

::-webkit-scrollbar-track {
  background: #1a1a1a;
}

::-webkit-scrollbar-thumb {
  background: #7fff00;
  border-radius: 3px;
}

::-webkit-scrollbar-thumb:hover {
  background: #99ff33;
}

input[type="text"],
input[type="number"],
input[type="password"] {
  background-color: #1a1a1a;
  border: 1px solid #3a3a3a;
  border-radius: 8px;
  color: #e0e0e0;
  font-family: 'Inter', sans-serif;
  font-size: 0.9rem;
  padding: 0.5rem 0.7rem;
  outline: none;
  transition: border-color 0.2s;
}

input[type="text"]:focus,
input[type="number"]:focus,
input[type="password"]:focus {
  border-color: #7fff00;
}

button {
  font-family: 'Inter', sans-serif;
  cursor: pointer;
  transition: background-color 0.15s, opacity 0.15s;
}

button:hover:not(:disabled) {
  filter: brightness(1.2);
}

button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/Utku ŞAHİN/Desktop/Dİscord" && npm install @fontsource/inter 2>&1 | tail -3 && node -e "require('@fontsource/inter/400.css')" 2>&1 || echo "fontsource check done" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>@fontsource/inter installed. theme.ts exists with color, font, and radius constants. renderer.ts imports fontsource CSS for weights 400/500/600/700. index.css uses Inter font-family and 8px border-radius globally. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 9 component style objects to use Inter font, 8px border-radius, and theme constants</name>
  <files>src/renderer/components/JoinServer.tsx, src/renderer/components/Lobby.tsx, src/renderer/components/RoomList.tsx, src/renderer/components/ChatPanel.tsx, src/renderer/components/VoiceControls.tsx, src/renderer/components/RoomMembers.tsx, src/renderer/components/InvitePanel.tsx, src/renderer/components/VolumePopup.tsx, src/renderer/components/ConnectionToast.tsx</files>
  <action>
**EXECUTOR NOTE (scope: 12 files):** This task touches 9 component files. Process them ONE AT A TIME. After updating each component, run a targeted grep on that file for leftover `monospace`, `Courier`, `'3px'`, and `'4px'` before moving to the next. This catches missed occurrences early rather than at the end.

For ALL 9 components, apply these systematic changes to each component's `const styles: Record<string, React.CSSProperties>` block:

**Global rules for every component:**
1. **REMOVE** every `fontFamily: 'monospace'` and `fontFamily: "'Courier New', Courier, monospace"` reference. The body now sets Inter globally, so component-level fontFamily is unnecessary.
2. **REPLACE** every `borderRadius: '3px'` and `borderRadius: '4px'` with `borderRadius: '8px'` (or `'6px'` for small elements like badges, dots, tags).
3. **Add `fontWeight: 600`** to headers/labels where `fontFamily: 'monospace'` was previously the only style distinction (replaces the monospace "bold" look with Inter semi-bold).
4. **Reduce `letterSpacing`** from `'0.1em'` to `'0.05em'` on uppercase headers (Inter is a proportional font, needs less tracking than monospace).
5. **Import theme** at the top of each component: `import { theme } from '../theme';` -- BUT this is optional for this plan. The executor may choose to either import theme constants or inline the color values directly. Both are acceptable. The key requirement is removing monospace and updating border-radius.

**Per-component specifics:**

**JoinServer.tsx:**
- `title`: Remove `fontFamily: 'monospace'`, add `fontWeight: 700`
- `label`: Remove `fontFamily: 'monospace'`, add `fontWeight: 500`
- `input`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`
- `avatarLabel`: Remove `fontFamily: 'monospace'`, add `fontWeight: 500`
- `avatarCell`: Change `borderRadius` from `'6px'` to `'8px'`
- `button`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`, add `fontWeight: 600`
- `error`: Remove `fontFamily: 'monospace'`

**Lobby.tsx:**
- `wrapper`: Remove `fontFamily: 'monospace'`
- All other styles are fine (colors and layout are already correct)

**RoomList.tsx:**
- `header`: Remove `fontFamily: 'monospace'`, add `fontWeight: 600`, change `letterSpacing` to `'0.05em'`
- `roomRow`: Change `borderRadius` from `'4px'` to `'8px'`
- `roomName`: Remove `fontFamily: 'monospace'`, add `fontWeight: 500`
- `count`: Remove `fontFamily: 'monospace'`
- `leaveBtn`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`, add `fontWeight: 600`

**ChatPanel.tsx:**
- `chatMsg`: Change `borderRadius` from `'4px'` to `'8px'`
- `input` (the textarea): Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`
- `uploadBtn`: Change `borderRadius` from `'4px'` to `'8px'`
- `sendBtn`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`
- `uploadError`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`
- `inlineImage`: Change `borderRadius` from `'4px'` to `'8px'`
- `fileCard`: Change `borderRadius` from `'4px'` to `'8px'`
- `fileLink`: Remove `fontFamily: 'monospace'`
- `lightboxImage`: Change `borderRadius` from `'4px'` to `'8px'`
- `lightboxClose`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`

**VoiceControls.tsx:**
- `iconBtn`: Change `borderRadius` from `'4px'` to `'8px'`
- `pttBtn`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`, add `fontWeight: 500`
- `rebindBtn`: Remove `fontFamily: 'monospace'`

**RoomMembers.tsx:**
- `item`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`
- `empty`: Remove `fontFamily: 'monospace'`

**InvitePanel.tsx:**
- `header`: Remove `fontFamily: 'monospace'`, add `fontWeight: 600`, change `letterSpacing` to `'0.05em'`
- `expiryBtn`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'3px'` to `'8px'`
- `fieldLabel`: Remove `fontFamily: 'monospace'`, add `fontWeight: 500`
- `input`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'3px'` to `'8px'`
- `copyBtn`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'4px'` to `'8px'`
- `preview`: Remove `fontFamily: 'monospace'`, change `borderRadius` from `'3px'` to `'8px'`
- `error`: Remove `fontFamily: 'monospace'`

**VolumePopup.tsx:**
- `popup`: Change `borderRadius` from `'6px'` to `'8px'`
- `header`: Remove `fontFamily: 'monospace'`
- `percent`: Remove `fontFamily: 'monospace'`

**ConnectionToast.tsx:**
- `toast`: Change `borderRadius` from `'6px'` to `'8px'`
- `text`: Remove `fontFamily: 'monospace'`

**CRITICAL verification after all changes:**
Search the entire `src/renderer/components/` directory for any remaining `fontFamily: 'monospace'`, `fontFamily: "'Courier New"`, `borderRadius: '3px'`, or `borderRadius: '4px'`. There must be ZERO matches. If any remain, fix them.

Also search `src/index.css` for `Courier` -- must be zero matches.
  </action>
  <verify>
    <automated>cd "C:/Users/Utku ŞAHİN/Desktop/Dİscord" && echo "--- Checking for leftover monospace ---" && grep -rn "monospace\|Courier" src/renderer/components/ src/index.css 2>&1 || echo "CLEAN: no monospace/Courier found" && echo "--- Checking for old borderRadius ---" && grep -rn "borderRadius.*'3px'\|borderRadius.*'4px'" src/renderer/components/ 2>&1 || echo "CLEAN: no 3px/4px borderRadius found" && echo "--- TypeScript check ---" && npx tsc --noEmit 2>&1 | head -20 && echo "--- Build check ---" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 9 components have fontFamily: 'monospace' removed. All borderRadius values updated to 8px (or 6px for small elements). No Courier or monospace references remain in renderer components or index.css. Headers use fontWeight 600. Uppercase headers have letterSpacing 0.05em. TypeScript compiles and build succeeds. The app presents a polished Discord Dark Modern aesthetic with Inter font throughout.</done>
</task>

</tasks>

<verification>
1. `npm ls @fontsource/inter` shows the package is installed
2. `src/renderer/theme.ts` exists with exported `theme` constant
3. `src/renderer.ts` has `@fontsource/inter` CSS imports
4. `src/index.css` uses `font-family: 'Inter', sans-serif` for body, input, and button
5. Zero occurrences of `monospace` or `Courier` in `src/renderer/components/` and `src/index.css`
6. Zero occurrences of `borderRadius: '3px'` or `borderRadius: '4px'` in `src/renderer/components/`
7. `npx tsc --noEmit` exits 0
8. `npm run build` exits 0
9. All components import from `../theme` if using theme constants (optional -- inline values also acceptable)
</verification>

<success_criteria>
- Inter font renders on every screen (join, lobby, room, chat, invite panel, volume popup, connection toast)
- No monospace/Courier font visible anywhere in the app
- All rounded corners are 8px (consistent, no mix of 3px/4px/8px)
- Color palette is consistent with the locked palette from CONTEXT.md
- The visual style is clean, dark, Discord-like with neon green accent
- TypeScript compiles and app builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-retro-ui-and-room-management/06-01-SUMMARY.md`
</output>
